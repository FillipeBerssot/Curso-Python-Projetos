# Ex. 04- Use a função len(string) para saber o tamanho de um texto (número de caracteres).
# Solução: Validação de dados em Python

texto = input('Digite aqui um texto qualquer: ')
tamanho_texto = len(texto)

print(f'O texto digitado tem {tamanho_texto} caracteres.')

